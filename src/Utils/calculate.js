const add = (first, second) => {
    return first + second;

}

const multiply = (first, second) => {
    return first * second;
}

const dicideTheFirstNumberByTheSecondNumber = (first, second) =>{
    return first / second
}

export {add, multiply, dicideTheFirstNumberByTheSecondNumber}

// export default add;
// const calculator = { add: add }
// export default calculator;